---
import octicons from "@primer/octicons";
import { NAV } from "@/data/nav";
import { PROFILE } from "@/data/profile";
import { SOCIALS } from "@/data/socials";

const pdfIcon = `
  <svg aria-hidden="true" class="sidebar-link-icon sidebar-pdf-icon" height="14" viewBox="0 0 16 16" width="14" xmlns="http://www.w3.org/2000/svg">
    <path d="M3.25 1.25h5.9l3.6 3.6v9.9h-9.5V1.25Z" fill="none" stroke="currentColor" stroke-linejoin="round" stroke-width="1.35"/>
    <path d="M9.15 1.25v3.6h3.6" fill="none" stroke="currentColor" stroke-linejoin="round" stroke-width="1.35"/>
    <text fill="currentColor" font-family="ui-monospace, SFMono-Regular, Menlo, Consolas, monospace" font-size="3.7" font-weight="800" letter-spacing="-.15" x="4.35" y="11.7">PDF</text>
  </svg>
`;
const socialIcons = {
  Email: octicons.mail.toSVG({ "aria-hidden": "true", class: "sidebar-link-icon", height: 14, width: 14 }),
  GitHub: octicons["mark-github"].toSVG({ "aria-hidden": "true", class: "sidebar-link-icon", height: 14, width: 14 }),
  Resume: pdfIcon,
  Twitter: octicons.mention.toSVG({ "aria-hidden": "true", class: "sidebar-link-icon", height: 14, width: 14 }),
};
---

<aside class="sidebar">
  <a class="skip-link" href="#content">skip to content</a>

  <nav class="sidebar-block" aria-label="Primary navigation">
    <ul class="nav-list sidebar-primary-nav">
      {NAV.map((item) => <li><a data-nav-link href={item.href}>{item.label}</a></li>)}
    </ul>
  </nav>

  <div class="sidebar-separator" aria-hidden="true"></div>

  <div class="sidebar-block">
    <ul class="nav-list">
      {
        SOCIALS.map((social) => (
          <li>
            <a
              href={social.href}
              rel="noreferrer"
              target={social.href.startsWith("mailto:") ? undefined : "_blank"}
            >
              <span set:html={socialIcons[social.label]} />
              {social.label.toLowerCase()}
            </a>
          </li>
        ))
      }
      <li>
        <a href="/pdf" target="_blank" aria-label="Open PDF resume">
          <span set:html={socialIcons.Resume} />
          resume
        </a>
      </li>
    </ul>
  </div>

  <div class="sidebar-separator" aria-hidden="true"></div>

  <div class="sidebar-block">
    <dl class="kv-list">
      <div>
        <dt>timezone</dt>
        <dd>{PROFILE.timezone}</dd>
      </div>
    </dl>
  </div>

  <div class="sidebar-separator" aria-hidden="true"></div>

  <div class="sidebar-block">
    <button
      class="theme-toggle"
      type="button"
      aria-label="Switch to dark mode"
      aria-pressed="false"
      data-theme-toggle
    >
      <span aria-hidden="true" data-theme-icon>◐</span>
      <span data-theme-option="light">light</span>
      <span aria-hidden="true" class="theme-separator">/</span>
      <span data-theme-option="dark">dark</span>
    </button>
  </div>
</aside>

<script>
  const themeToggle = document.querySelector<HTMLButtonElement>("[data-theme-toggle]");
  const themeIcon = document.querySelector<HTMLElement>("[data-theme-icon]");
  const themeOptions = Array.from(document.querySelectorAll<HTMLElement>("[data-theme-option]"));
  const themeColor = document.querySelector<HTMLMetaElement>('meta[name="theme-color"]');
  const storageKey = "polarzero.theme";

  const setTheme = (theme: "light" | "dark") => {
    const isDark = theme === "dark";

    document.documentElement.dataset.theme = theme;

    try {
      localStorage.setItem(storageKey, theme);
    } catch {
      // Theme still updates for the current page when storage is unavailable.
    }

    themeToggle?.setAttribute("aria-pressed", String(isDark));
    themeToggle?.setAttribute("aria-label", `Switch to ${isDark ? "light" : "dark"} mode`);

    themeOptions.forEach((option) => {
      option.toggleAttribute("data-active", option.dataset.themeOption === theme);
    });

    if (themeIcon) {
      themeIcon.textContent = isDark ? "◑" : "◐";
    }

    themeColor?.setAttribute("content", isDark ? "#d96f31" : "#f97316");
  };

  const currentTheme = document.documentElement.dataset.theme === "dark" ? "dark" : "light";
  setTheme(currentTheme);

  themeToggle?.addEventListener("click", () => {
    setTheme(document.documentElement.dataset.theme === "dark" ? "light" : "dark");
  });
</script>
